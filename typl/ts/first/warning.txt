1. После первой компиляции будет ошибка что-то про тип итератора, модуля es2015 и прочий рак.
   Установи эту хреновину, чтобы ошибка не появлялась:
    npm install @types/node

2. Для генерации tsconfig.json
    tsc --init

3. Для компиляции и последующего запуска введи
    ts-node index.ts